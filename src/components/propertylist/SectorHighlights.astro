---
// SectorHighlights.astro - DESHABILITADO - Solo para países con datos de API
// TODO: Este componente debe recibir datos reales de la API por país/región

export interface Props {
  sector: string;
  location: string;
  propertyType?: string;
  totalProperties: number;
  nearbyServices?: Array<{
    name: string;
    category: string;
    distance: string;
    rating?: number;
  }>;
  marketData?: {
    averagePrice: string;
    priceGrowth: string;
    connectivity: string[];
    demographics: string;
  };
  // Nuevos props requeridos de API
  sectorInfo?: {
    name: string;
    tagline: string;
    highlights: string[];
    lifestyle: string;
    connectivity: string[];
    demographics: string;
    futureProjects: string[];
    uniqueValue: string;
  };
}

const { sectorInfo } = Astro.props;

// Solo renderizar si hay datos reales de la API
if (!sectorInfo) {
  console.log('🚫 SectorHighlights: No se muestra - Requiere datos de API por país/región');
  return null;
}

console.log('✅ SectorHighlights: Renderizando con datos de API');
---

<!-- COMPONENTE DESHABILITADO -->
<!-- Este componente requiere datos específicos por país/región de la API -->
<!-- Para habilitarlo, la API debe incluir:
     - Información específica de sectores por país
     - Datos de mercado locales
     - Conectividad y servicios por región
     - Proyectos futuros específicos del país
-->

<div class="hidden">
  <!-- Contenido original comentado hasta tener datos reales de API -->
  <p>SectorHighlights requiere implementación de API por país</p>
</div>

<section class="py-16 bg-gradient-to-br from-blue-50 via-white to-indigo-50">
  <div class="container mx-auto px-4">
    <div class="max-w-6xl mx-auto">
      
      <!-- Header principal -->
      <div class="text-center mb-12">
        <div class="inline-flex items-center gap-2 px-4 py-2 bg-blue-100 text-blue-800 rounded-full text-sm font-medium mb-4">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
          </svg>
          Por qué elegir {currentSector.name}
        </div>
        
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          {currentSector.name}: {currentSector.tagline}
        </h2>
        
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          Descubre por qué {currentSector.name} es {currentSector.uniqueValue} y 
          la elección preferida de {currentSector.demographics}.
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
        
        <!-- Lado izquierdo: Beneficios principales -->
        <div>
          <h3 class="text-2xl font-bold text-gray-900 mb-6">
            Ventajas de Vivir en {currentSector.name}
          </h3>
          
          <div class="space-y-4 mb-8">
            {safeHighlights.map((highlight, index) => (
              <div class="flex items-start gap-4 p-4 bg-white rounded-lg shadow-sm hover:shadow-md transition-shadow">
                <div class="flex-shrink-0 w-8 h-8 bg-green-100 rounded-full flex items-center justify-center">
                  <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                  </svg>
                </div>
                <div>
                  <h4 class="font-semibold text-gray-900 mb-1">{highlight}</h4>
                </div>
              </div>
            ))}
          </div>

          <div class="bg-gradient-to-r from-green-50 to-emerald-50 p-6 rounded-xl border border-green-100">
            <h4 class="font-semibold text-green-900 mb-2">Estilo de Vida</h4>
            <p class="text-green-800 text-sm">{currentSector.lifestyle}</p>
          </div>
        </div>

        <!-- Lado derecho: Datos del mercado y conectividad -->
        <div>
          <h3 class="text-2xl font-bold text-gray-900 mb-6">
            Datos del Mercado y Conectividad
          </h3>
          
          {marketData && (
            <div class="bg-white p-6 rounded-xl shadow-sm mb-6">
              <h4 class="font-semibold text-gray-900 mb-4">Indicadores del Mercado</h4>
              <div class="grid grid-cols-2 gap-4">
                <div>
                  <span class="text-sm text-gray-600">Precio Promedio</span>
                  <div class="text-lg font-bold text-[#f04e00]">{marketData.averagePrice}</div>
                </div>
                <div>
                  <span class="text-sm text-gray-600">Crecimiento Anual</span>
                  <div class="text-lg font-bold text-green-600">{marketData.priceGrowth}</div>
                </div>
              </div>
              <div class="mt-4">
                <span class="text-sm text-gray-600">Demografía Principal</span>
                <p class="text-sm text-gray-800 mt-1">{marketData.demographics}</p>
              </div>
            </div>
          )}

          <div class="bg-white p-6 rounded-xl shadow-sm mb-6">
            <h4 class="font-semibold text-gray-900 mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Conectividad y Transporte
            </h4>
            <div class="space-y-2">
              {safeConnectivity.map(connection => (
                <div class="flex items-center gap-2 text-sm text-gray-700">
                  <svg class="w-4 h-4 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                  </svg>
                  {connection}
                </div>
              ))}
            </div>
          </div>

          <div class="bg-white p-6 rounded-xl shadow-sm">
            <h4 class="font-semibold text-gray-900 mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
              Proyectos Futuros
            </h4>
            <div class="space-y-2">
              {safeFutureProjects.map(project => (
                <div class="flex items-center gap-2 text-sm text-gray-700">
                  <svg class="w-4 h-4 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                  {project}
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>

      <!-- Servicios cercanos -->
      {Object.keys(categorizedServices).length > 0 && (
        <div class="mb-16">
          <h3 class="text-2xl font-bold text-gray-900 mb-8 text-center">
            Servicios y Lugares de Interés Cercanos
          </h3>
          
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
            {Object.entries(categorizedServices).map(([category, services]) => (
              <div class="bg-white p-5 rounded-xl shadow-sm hover:shadow-md transition-shadow">
                <h4 class="font-semibold text-gray-900 mb-3 flex items-center gap-2">
                  <span class="text-lg">{serviceIcons[category] || '📍'}</span>
                  {category}
                </h4>
                <div class="space-y-2">
                  {services.slice(0, 4).map(service => (
                    <div class="flex items-center justify-between text-sm">
                      <span class="text-gray-700">{service.name}</span>
                      <div class="flex items-center gap-1 text-xs text-gray-500">
                        {service.rating && (
                          <div class="flex items-center gap-1">
                            <svg class="w-3 h-3 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                              <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                            </svg>
                            <span>{service.rating}</span>
                          </div>
                        )}
                        <span>{service.distance}</span>
                      </div>
                    </div>
                  ))}
                  {services.length > 4 && (
                    <div class="text-xs text-gray-500 mt-2">
                      +{services.length - 4} más en la zona
                    </div>
                  )}
                </div>
              </div>
            ))}
          </div>
        </div>
      )}

      <!-- CTA específico del sector -->
      <div class="bg-gradient-to-r from-[#f04e00] to-[#ff7a3d] p-8 rounded-2xl text-center text-white">
        <h3 class="text-2xl font-bold mb-4">
          ¿Listo para vivir en {currentSector.name}?
        </h3>
        <p class="text-lg mb-6 opacity-90">
          Descubre las {totalProperties} {propertyType || 'propiedades'} disponibles en {currentSector.name} 
          y encuentra tu hogar ideal en {currentSector.uniqueValue}.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href={`/contacto?sector=${sector}&tipo=${propertyType || ''}`}
            class="inline-block px-8 py-3 bg-white text-[#f04e00] rounded-lg hover:bg-gray-100 transition-colors font-semibold"
          >
            Hablar con un Especialista
          </a>
          <a 
            href={`#propiedades-${sector}`}
            class="inline-block px-8 py-3 bg-white/20 text-white border border-white/30 rounded-lg hover:bg-white/30 transition-colors font-semibold"
          >
            Ver Propiedades Disponibles
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Schema.org structured data -->
<script type="application/ld+json" set:html={JSON.stringify({
  "@context": "https://schema.org",
  "@type": "Place",
  "name": currentSector.name,
  "description": currentSector.tagline,
  "address": {
    "@type": "PostalAddress",
    "addressLocality": currentSector.name,
    "addressRegion": location,
    "addressCountry": "DO"
  },
  "amenityFeature": currentSector.highlights.map(highlight => ({
    "@type": "LocationFeatureSpecification",
    "name": highlight
  }))
})} />